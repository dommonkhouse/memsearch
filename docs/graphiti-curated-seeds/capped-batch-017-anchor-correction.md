# Capped batch 017 anchor correction relationships

Reviewed on 2026-06-14 for MON-316 capped Graphiti Superpowers-plan tuning.

## Graphiti sidecar anchors

optional derived is the Graphiti sidecar boundary anchor.
Markdown canonical is the MemSearch source-of-truth anchor.
Milvus primary rebuildable index is the MemSearch recall anchor.
explicit CLI is the first Graphiti command surface anchor.
prompt injection is deferred until the Graphiti pilot proves useful.

## Kuzu deferral anchors

Kuzu is the deferred backend anchor.
archived is the KuzuDB repository-risk anchor.
crash is the Graphiti Kuzu add-episode issue anchor.
FalkorDB is the first pilot backend anchor.

## Capped ingest anchors

`ms_memsearch_active_curated_v1` is the capped ingest group anchor.
`.memsearch/graphiti-curated-manifest.json` is the capped ingest manifest anchor.
dry-run, cap, manifest checkpoint, evaluation, and rollback are the capped ingest safety anchors.
no full `.memsearch/memory` ingestion is the capped ingest boundary anchor.

## Chat backfill anchors

source-normalisation is the chat backfill pipeline anchor.
canonical markdown is the generated chat memory anchor.
manifest is the repeatable incremental run anchor.
Milvus derived index is the chat backfill search anchor.

## Manus recall anchors

raw Manus export is the source-of-truth lane anchor.
sanitised full Markdown is the evidence lane anchor.
compact session cards are the practical MemSearch recall lane anchor.
raw event logs are the rejected canonical ingestion anchor.
too slow and low-value embeddings are the rejection-reason anchors.

## Source freshness anchors

Linear daily is the source freshness issue/comment card anchor.
Manus weekly is the source freshness task-card anchor.
source cards are written into `.memsearch/memory/`.
dry-run is the scheduler approval boundary anchor.
client-side diff is the Manus weekly sync anchor.
refuse silent full export is the Manus safety anchor.
